public class Ejercicio {
    public static void main(String[] args) {
        // Area del cuadrado
        // Declaracion de los variables
        int lado, area;
        // Asignamos un valor a la variable lado
        lado = 3;
        // Calculamos el area del cuadrado
        area = lado * lado;
        // Mostramos por pantalla la respuesta
        System.out.println(" El area del cuadrado:" + area);
    }
}